import type { Metadata } from "next"; import Link from "next/link"; import { getCommandAccess, accessMessage } from "../_lib/access"; import { CommandCenterShell } from "../_components/CommandCenterShell"; import AskLearnClient from "./AskLearnClient"; import styles from "./ask.module.css";
export const metadata:Metadata={title:"Ask & Learn | My Hands Gifted",robots:{index:false,follow:false}};
export default async function AskLearnPage(){const access=await getCommandAccess();if(access.state!=="ready"){const m=accessMessage(access.state);return <div className={styles.access}><h1>{m.title}</h1><p>{m.body}</p><Link href="/">Return</Link></div>}return <CommandCenterShell context="personal"><main className={styles.page}><Link href="/command-center/workspace" className={styles.back}>← My Hands Gifted</Link><span className={styles.kicker}>ASK & LEARN</span><h1>Ask the question. Get the information back.</h1><p className={styles.lead}>Use this for learning, research, guidance and developing an idea. The answer stays on screen so you can respond, follow up, or decide what belongs in Hands Gifted.</p><AskLearnClient/></main></CommandCenterShell>}